# V100 training resume supporting evidence

This bundle contains the retained ZeRO-2/ZeRO-3 resume rows and the associated
ZeRO-3 run log that were previously stored as standalone files in `bench/`.
They are supporting training-contract evidence; current performance claims use
the dedicated V100 benchmark bundles.

The raw JSONL and log payloads were moved without modification.
